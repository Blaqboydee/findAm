export const metadata = {
  title: 'Onboarding - FindAm',
};

export default function OnboardingLayout({ children }) {
  return (
    <section className="min-h-screen">
      {children}
    </section>
  );
}
